<div class="{{ block }}">
	<div class="{{ block.elem('middle') }}">
		<div class="block.elem('row')">
			<div class="col">
				<h1>Быстрый логин</h1>
				<p>Мой друг, кто ты?</p>
				{% for item in list %}
					<a href="/profile/{{ item.id }}/">{{ item.name }}</a>
				{% endfor %}
			</div>
		</div>
	</div>
</div>